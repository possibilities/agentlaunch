{
  "name": "agentsurface",
  "version": "0.1.0",
  "description": "One launcher for agent harnesses: open and resume claude, codex, and pi in place today, land them on a surface tomorrow",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/possibilities/agentsurface.git"
  },
  "type": "module",
  "bin": {
    "agentsurface": "./src/main.ts"
  },
  "engines": {
    "bun": ">=1.3.14"
  },
  "scripts": {
    "lint": "biome check .",
    "format": "biome check --write src test scripts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "generate:schema": "bun run scripts/generate-schema.ts && biome check --write catalog.schema.json",
    "check": "bun run lint && bun run typecheck && bun run test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.3",
    "@types/bun": "1.3.14",
    "bun-types": "1.3.14",
    "typescript": "5.9.3"
  },
  "dependencies": {
    "zod": "^4"
  }
}
